import { combineReducers } from '@reduxjs/toolkit'
// Import các slice khi đã tạo
// import authReducer from '@/features/auth/authSlice'

const rootReducer = combineReducers({
  // auth: authReducer,
  // Thêm các reducer khác
})

export default rootReducer